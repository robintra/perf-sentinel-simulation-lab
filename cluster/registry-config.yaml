# Registry mirror configuration. Empty placeholder for sprint S1.
#
# In sprint S3 (when GitLab CE arrives), populate this file with mirror
# definitions, then reference it from k3d-config.yaml via:
#
# registries:
#   config: |
#     mirrors:
#       "ghcr.io":
#         endpoint:
#           - "http://gitlab-registry.ci.svc.cluster.local"
#
# For sprint S1 the cluster pulls images directly from upstream registries
# (Docker Hub, ghcr.io, quay.io) over the host network.
