# Baked defaults. Real container env vars (injected by the helm deployment) take
# precedence. OTel config is set as real ENV in the Dockerfile (the SDK autoloader
# reads getenv() before Symfony loads this file).
APP_ENV=prod
APP_DEBUG=0
APP_SECRET=9f2c1a7b4e6d8c0f3a5b7d9e1c2f4a6b

DB_HOST=localhost
DB_PORT=5432
DB_NAME=lab
DB_USER=symfony_user
DB_PASSWORD=lab_symfony
DB_SCHEMA=symfony
